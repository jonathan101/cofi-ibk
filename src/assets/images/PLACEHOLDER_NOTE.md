# Placeholder Note

## gradient-promo.png

Actualmente se está usando `gradient-promo.svg` como placeholder.

Para reemplazar con una imagen PNG real:
1. Crear una imagen PNG de 400x200px con el gradiente de marca Interbank
2. Guardar como `gradient-promo.png` en este directorio
3. Actualizar las referencias en los componentes si es necesario
4. Eliminar `gradient-promo.svg` si ya no se necesita

## avatar-chicho.png

Si se necesita una versión PNG del avatar de Chicho:
1. Exportar `avatar-chicho.svg` a PNG en las dimensiones deseadas (recomendado: 128x128px o 256x256px)
2. Guardar como `avatar-chicho.png` en este directorio
3. Usar la versión PNG donde se requiera mejor rendimiento o compatibilidad
